[build-system]
requires = ["setuptools==75.8.0", "pybind11==2.13.6", "cmake==3.30.5"]
build-backend = "setuptools.build_meta"

[project]
name = "evtq"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
]
